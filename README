How to build:

1) Build `godot-cpp` bindings
- git submodule update --init --recursive
- cd src/godot-cpp
- scons platform=<your_platform> generate_bindings=yes

2) Build game scripts
- make sure you're in the top level folder
- scons platform=<your_platform>

3) Run game in Godot

Alternatively, the project has been exported to the `bin/` directory.

Source code:
https://github.com/vmorandi/Project2/
